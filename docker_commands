docker-compose up -d
docker-compose up -d --build


docker-compose down

docker-compose exec omni bash



docker-compose ps

sudo service docker restart

docker system prune -f


sudo chown -R 2000:2000 /opt/ros/noetic/flightSoftware
sudo chmod 666 /dev/i2c-1
